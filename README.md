# Three.js Backend Command Center Portfolio

Interactive 3D portfolio variant for Jaya Krishna Polaki.

```bash
npm install
npm run dev
npm run build
npm run preview
```

Deploy the `dist/` folder to GitHub Pages. The project assumes the repo path `/portfolio-threejs/`.
